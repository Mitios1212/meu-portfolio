var nome = "Mit";
var x = nome.length;

resultado = x > 5 ? "obrigado" :  "Digite seu nome"

console.log(resultado);

var s = "JavaScript e Python"
lugar =(s.indexOf("Python"));

console.log(lugar);

var frase = "fala boi"
if(frase.startsWith("fala")){
    let sub = frase.substring(2);
    let novaFrase = sub.replace("boi","Chico");
    let novaFraseMaiuscula = novaString.toUpperCaser();
    console.log(novaFraseMaiuscula);

}

